{# {% if featured_products|length %} #}
{# <h2>{{ 'Featured products'|t }}</h2> #}
{# {% endif %} #}
{# <div class="carousel_fullwidth carousel_desktop js-main-carousel-quaker"> #}
{# {% for product in featured_products %} #}
{# <div class="banner_slide home_slide" data-nid="{{ nid }}"> #}
{# <div class="banner-desktop"> #}
{# <div class="quaker-featured--container"> #}
{# <div class="quaker-feature--display"> #}
{# <div class="quaker-feature--leftcol"> #}
{# <img class="product-image" src="{{ product.extra_image_url }}"> #}
{# </div> #}
{# </div> #}
{# <div class="quaker-coupon--text"> #}
{# <div class="quaker-banner_slide--text"> #}
{# <span class="product-title"> {{ product.title|raw }}</span> #}
{# {% set trimmed_body = truncateText(product.body|raw, 20) %} #}
{# #}{# <span class="quaker-product-description"> {{product.body|raw}}</span> #}
{# <br> #}
{# <div class="quaker-btn"> #}
{# <a href="{{ product.link }}" title="{{ product.title | raw }}"> #}
{# {{ 'View Product'|t }} #}
{# </a> #}
{# </div> #}
{# </div> #}
{# </div> #}
{# </div> #}
{# </div> #}

{# </div> #}
{# {% endfor %} #}
{# </div> #}


<div class="container">
  <div class="row">

    <div class="col-md-12 featured-prod-heading text-center">
      {% if featured_products|length %}
        <h1>{{ 'Featured Products'|t }}</h1>
      {% endif %}
    </div>

  </div>
</div>


<div id="quaker-featured-products-carousel" class="carousel slide" data-ride="carousel">
  <ol class="carousel-indicators quaker-carousel-indicators">
    {% for idx, product in featured_products %}
      <li data-target="#quaker-featured-products-carousel" data-slide-to="{{ idx }}" class="{{ idx==0 ? 'active' :'' }}"></li>
    {% endfor %}
  </ol>
  <div class="carousel-inner container">
    {% for idx, product in featured_products %}
      <div class="row carousel-item {{ idx==0 ? 'active' :'' }}">
        <div class="col-md-3 quaker-featured-prod-img-col quaker-featured-prod-img-col-{{ idx }}">

          <img loading="lazy" class="" src="{{ product.extra_image_url }}" alt="{{ product.title | raw }}">


          <div class="quakers-featured-product-description">
            <h4>
              {{ product.title|raw }}<br>
              {% set trimmed_body = truncateText(product.body|raw, 20) %}
            </h4>
          </div>

          <div class="quaker-view-featured-products">
            <a class="btn_quaker" href="{{ product.link }}" title="{{ product.title | raw }}">
              {{ 'View Product'|t }}
            </a>
          </div>

        </div>
      </div>

    {% endfor %}
  </div>

  <a class="carousel-control-prev" href="#quaker-featured-products-carousel" role="button" data-slide="prev" aria-label="toggle arrow">
    <span class="carousel-control-prev-icon"></span>
  </a>
  <a class="carousel-control-next" href="#quaker-featured-products-carousel" role="button" data-slide="next" aria-label="toggle arrow">
    <span class="carousel-control-next-icon"></span>
  </a>

</div>
